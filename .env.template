# ===========================================
# Fiber CRUD API - 환경 변수 설정
# ===========================================
# 이 파일을 .env로 복사하고 <> 부분을 실제 값으로 채워주세요
# cp .env.template .env
#
# 민감한 정보는 절대 Git에 커밋하지 마세요!
# ===========================================

# 서버 설정
API_PORT=8080

# 데이터베이스 설정
DATABASE_PATH=./database.db

# JWT 설정
# 보안을 위해 강력한 랜덤 문자열을 사용하세요
# 생성 방법: openssl rand -base64 32
JWT_SECRET_KEY=<your-jwt-secret-key-here>

# CORS 설정
# 프론트엔드 주소 (여러 개일 경우 쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:3000

# ===========================================
# Logto OIDC 설정
# ===========================================
# Logto 콘솔(https://cloud.logto.io/beh25r)에서
# Application을 생성한 후 아래 값을 설정하세요
#
# 1. Applications > Create Application > Traditional Web
# 2. Application Details에서 아래 정보 복사
# ===========================================

# Logto 엔드포인트 (예: https://beh25r.logto.app)
LOGTO_ENDPOINT=<your-logto-endpoint>

# Application ID (App Details에서 복사)
LOGTO_APP_ID=<your-app-id>

# Application Secret (App Details에서 복사)
LOGTO_APP_SECRET=<your-app-secret>

# Redirect URI (콜백 URL)
# Logto 콘솔의 Redirect URIs에 이 값을 추가하세요
LOGTO_REDIRECT_URI=http://localhost:8080/oidc/callback

# Post Logout Redirect URI (로그아웃 후 리다이렉트)
# Logto 콘솔의 Post Logout Redirect URIs에 이 값을 추가하세요
LOGTO_POST_LOGOUT_REDIRECT_URI=http://localhost:3000

# 개발 환경 설정
# development, production
ENVIRONMENT=development

# ===========================================
# 설정 완료 체크리스트
# ===========================================
# [ ] JWT_SECRET_KEY 생성 및 설정
# [ ] Logto Application 생성 완료
# [ ] LOGTO_ENDPOINT 설정
# [ ] LOGTO_APP_ID 설정
# [ ] LOGTO_APP_SECRET 설정
# [ ] Logto 콘솔에 Redirect URIs 등록
# [ ] Logto 콘솔에 CORS Allowed Origins 등록
# [ ] Logto에서 admin, user Roles 생성
# [ ] 이 파일을 .env로 복사
# ===========================================
